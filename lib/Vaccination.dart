import 'package:flutter/material.dart';

class Vaccination extends StatelessWidget {
  const Vaccination({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Test Vaccination Content'),
      ),
    );
  }
}